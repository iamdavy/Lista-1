#include <stdio.h>
#include <stdlib.h>
/*Escreva um algoritmo que leia um número não determinado de pares de valores [m,n], onde m e n
são inteiro e positivos. Deve-se ler um par de cada vez. Mostre a soma de todos os números inteiros
de m e n (inclusive m e n). A entrada de pares terminará quando m for igual a n. */
int main()
{

    return 0;
}
