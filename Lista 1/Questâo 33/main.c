#include <stdio.h>
#include <stdlib.h>

int main()
{
    int lado;

    printf("Insira o lado do quadrado: ");
    scanf("%d", &lado);

    printf("A area do quadrado e: %d", lado * lado);
    return 0;
}
